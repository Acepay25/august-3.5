# Plan: Fix Syntax Error + Modularization Refactoring

## Phase 0: Fix App.tsx Syntax Error (BLOCKING)
**Error:** `Missing catch or finally clause` at line 1894 in `startPostMortemAnalysis`. A `try {` block has no matching `catch`/`finally` — likely caused by the earlier dead-code removal shifting braces. Read the area around line 1894, find the orphaned `try`, and restore the missing `catch/finally` block. Verify with `npm run dev`.

## Phase 1: Move test files out of services/
Move 6 test files + 2 output artifacts from `services/` to `tests/`:
- `standalone-test-rl.ts`, `test-bayesian.ts`, `test-memory-consolidation.ts`, `test-rl.ts`, `test-rl.cjs`, `test-rule-extraction.ts`, `test-rule-extraction.cjs`, `test_structured_rules.ts`, `test_mem_output.txt`, `test_output.txt`

## Phase 2: Organize services into domain subdirectories
Create 7 subdirectories and move files:
- `services/providers/` — 11 LLM provider services (gemini, deepseek, openai, groq×3, openrouter, grokNative, zhipu, ensemble) + `accuracy/`
- `services/analysis/` — 12 market/technical services (TechnicalAnalysis, MarketData, HybridIntelligence, NumericChart, AITrendline, TimeframeConfluence, ScalpDetection, EntryTiming, PatternClassification, ProbabilityEngine, MonteCarlo, CorrelationRisk)
- `services/learning/` — 16 learning/memory services
- `services/validation/` — 7 validation/gating services
- `services/backtesting/` — 5 backtesting/performance services
- `services/infrastructure/` — 10 persistence/platform services
- `services/ui/` — 9 UI-adjacent services

Update ALL import paths across the codebase (App.tsx, components, other services). Use parallel agents per domain.

## Phase 3: Organize components into feature folders
Create 8 subdirectories and move files:
- `components/chat/` — ChatArea, ChatInput, MessageItem, QuickActionChips, ConversationHistory
- `components/analysis/` — AnalysisResult, AnalysisProgress, DebateView, LiveAnalysisView, LivePostMortemView, ConfluenceScoreIndicator, HybridDataPanel, VisionDataViewer
- `components/market/` — LiveMarket, LiveMarketDataView, OKXChart, ProbabilityWidget
- `components/journal/` — Journal, TradeLog, LogTradeModal, UpdateTradeModal, SavedAnalyses, PerformanceReview
- `components/dashboards/` — WinRate, ModelPerformance, VersionHistory, LearningDashboard, AdvancedAnalyticsSidePanel
- `components/modals/` — All modal dialogs (7 files)
- `components/settings/` — Settings, SettingsMenu, AnalystLensSettings, UserProfileManager
- `components/shared/` — Header, Icons, Toast, ErrorBoundary, ImagePreview, UpdateNotification, MistakeWarningBanner, StrategySearch

Update ALL import paths.

## Phase 4: Split types.ts into domain modules
Create `types/` directory:
- `types/enums.ts` — MessageRole, TradeOutcome, AIProvider, AnalystRole
- `types/message.ts` — Message, DebateTurn, GroundingChunk, ImageMetadata
- `types/analysis.ts` — TradeAnalysis, MarketConditions, EntryPoint, KeyLevels, etc.
- `types/trade.ts` — LoggedTrade, TradeSummary, SavedAnalysis, Conversation
- `types/learning.ts` — GlobalMemory, LearningRule, InsightKnowledgeBase, etc.
- `types/calibration.ts` — CalibrationEntry, ConfidenceCalibration, GranularCalibration
- `types/market.ts` — Kline, ConfluenceData, BacktestResult
- `types/user.ts` — UserProfile, UserSettings, CustomInstruction
- `types/lens.ts` — AnalystLensConfig, TradingStyle
- `types/progress.ts` — AnalysisStep, AnalysisSubStep
- `types/index.ts` — Barrel re-export

Keep root `types.ts` as a re-export shim: `export * from './types/index';`

## Phase 5: Split constants/prompts.ts
Split 1,657-line file into:
- `constants/prompts/analysisPrompts.ts`
- `constants/prompts/debatePrompts.ts`
- `constants/prompts/memoryPrompts.ts`
- `constants/prompts/learningPrompts.ts`
- `constants/prompts/index.ts` — Barrel

Keep `constants/prompts.ts` as re-export shim.

## Phase 6: Extract hooks from App.tsx
Extract 9 custom hooks, reducing App.tsx from 3,912 to ~400 lines:
- `hooks/useUserProfiles.ts` — user CRUD, login, import/export
- `hooks/useConversations.ts` — conversation state, per-conv settings
- `hooks/useAnalysisPipeline.ts` — handleSendMessage, provider routing
- `hooks/usePostMortem.ts` — post-mortem flow
- `hooks/useTradeLogging.ts` — trade logging, data capture, update
- `hooks/useMemorySystem.ts` — global memory, calibration, rules
- `hooks/useMarketData.ts` — hybrid intelligence, connection
- `hooks/useUIState.ts` — ~25 modal/panel booleans
- `hooks/usePersistence.ts` — auto-save, load, reset

App.tsx becomes a thin composition root that calls these hooks and renders components.

## Phase 7: Verify
- `npm run dev` — confirm no import errors, app loads
- Check all views render correctly
- Commit each phase independently

## Expected Outcome
| Metric | Before | After |
|--------|--------|-------|
| App.tsx | 3,912 lines | ~400 lines |
| types.ts | 825 lines (1 file) | 10 files, 60-120 lines each |
| prompts.ts | 1,657 lines (1 file) | 4 files, 300-500 lines each |
| services/ | 75 files flat | 7 domain folders |
| components/ | 49 files flat | 8 feature folders |
| hooks/ | 2 files | 11 files |
| Largest file | 3,912 lines | ~600 lines |