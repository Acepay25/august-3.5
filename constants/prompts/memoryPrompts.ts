export const AI_PROVIDER_MEMORY_ENFORCEMENT_PROMPT = `
**MEMORY:**
Use retrieved notes and skills only when they match this coin, direction, or regime. Cite a specific trade or skill when you use one. If nothing matches, say so once and move on — do not invent history or force a citation.
`;
